version: "3.8"

services:
  cloudflare:
    image: "cloudflare/cloudflared:latest"
    restart: "always"
    command:
      - "tunnel"
      - "--no-autoupdate"
      - "run"
      - "--token"
      - "${CLOUDFLARE_TOKEN}"

networks:
  default: