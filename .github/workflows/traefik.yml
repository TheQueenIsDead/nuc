on:
  push:
    branches:
      - 2024

jobs:
  traefik:
    runs-on: self-hosted
    steps:
      - run: docker ps

      - uses: actions/checkout@v4

      - run: docker compose -f docker/traefik.yml up --wait
