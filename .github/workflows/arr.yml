on:
  push:
    paths:
      - .github/workflows/arr.yml
      - docker/arr.yml
    branches:
      - main

jobs:
  arr:
    runs-on: self-hosted
    env:
      CONFIG_FOLDER: "/home/tqid/media/config"
      DOWNLOADS_FOLDER: "/home/tqid/media/downloads"
      MEDIA_FOLDER: "/home/tqid/media/media"
    steps:
      - uses: actions/checkout@v4
      - run: docker compose -f docker/arr.yml up --wait
